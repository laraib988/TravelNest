const fs = require('fs'); let c = import { headers } from 'next/headers';\n\nexport const getDynamicAlternates = async (path) => {\n  const headersList = await headers();\n  const locale = headersList.get('x-locale') || 'en';\n  const p = path.startsWith('/') ? path : \/\\;\n  const base = p === '/' ? '' : p;\n  return {\n    canonical: \https://www.vaitour.com/\\\,\n    languages: {\n      en: \https://www.vaitour.com/en\\,\n      ja: \https://www.vaitour.com/ja\\,\n      ur: \https://www.vaitour.com/ur\\,\n      fr: \https://www.vaitour.com/fr\\,\n      ar: \https://www.vaitour.com/ar\\,\n      'x-default': \https://www.vaitour.com/en\\,\n    }\n  };\n};\n\n + fs.readFileSync('src/lib/seo.ts', 'utf8'); fs.writeFileSync('src/lib/seo.ts', c);
